CREATE TABLE ARCHITECTURE (
    name VARCHAR,
	id INTEGER,
    parent VARCHAR,
    child DATE,
    PRIMARY KEY (name)
);

CREATE TABLE PHYSICS (
    name VARCHAR,
	id INTEGER,
    parent VARCHAR,
    child DATE,
    PRIMARY KEY (name)
);